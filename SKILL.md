---
name: ai-video-style-remake
description: "把任意已有视频片段重绘成任意视觉风格（赛博朋克、废土、霓虹都市、古典宫廷、胶片感等），或做成外语版（韩语/泰语/英语）二创短视频。当用户说「AI重绘这段视频」「风格迁移」「换个世界观」「做成海外版/外语版」「二创」「转绘」「译制」时使用。内含分工原则、路线决策、五步闭环、分层提示词法、模型选型标准、改与重跑的判断、收尾剪辑清单。"
agent_created: true
---

# 视频二创：风格重绘 / 外语版

把一段已有的视频，变成另一种视觉风格或另一种语言的成片。适用于影视剧片段、短剧、口播、UGC 素材。

---

## 三条铁律（先记这个，能省一半返工）

**1. 喂原片重绘，不用纯文字生成。**
纯靠提示词生成，人是模型现画的——脸不是本人，一动起来更不像；动作和表演也是它自己演的，什么时候逼近、哪一句拔高音量，它全不知道，节奏必然对不上。喂原片重绘则是在本人画面上换画风，**脸是本脸，动作、走位、节奏天然跟原片一致，每段长相也天然统一**，提示词只需要写「改成什么样」，不用描述怎么演。

**2. AI 负责把画面生出来，精细活你自己做。**
冗帧、乱码台词、音画不同步这类问题，跟模型反复扯皮既费时间又费积分，回到剪辑软件通常几十秒解决。**早认命，早收工。**

**3. 全程锁定同一套设置。**
选定模型和提示词后跑完全部片段，中途换会让前后段风格串味。

---

## 第 0 步：选路线

| 判断点 | 重绘（换世界观） | 译制（换语言） |
| --- | --- | --- |
| 画面 | AI 重画，服装/场景/氛围变掉 | 保留原画面 |
| 表演 | 能保住动作表情，但脸可能飘 | 完整保留 |
| 成本 | 中到高，按段消耗额度 | 低 |
| 速度 | 慢，要逐段跑 | 快 |
| 声音 | 大多不带原声，需回剪辑软件补 | 工具自带对口型配音 |
| 查重 | 画面全新，更安全 | 画面与原片一致，风险较高 |

- 要视觉冲击、要规避查重 → **重绘**
- 预算有限、想保住原表演、只是换语种 → **译制**
- 拿不准 → 先跑通译制流程，再上重绘

译制路线只需三步：上传原片选目标语言 → 挑对口型效果好的工具（韩语/日语/泰语这类亚洲语言尤其要挑）→ 回剪辑软件换字幕、台标、色调、BGM。**下文五步针对重绘路线。**

---

## 第一步：备料与切段

1. **拿到源视频**：优先从原链接保存；只能录屏时，Mac 录屏默认落在 `~/Desktop/录屏YYYY-MM-DD HH.MM.SS.mov`。
2. **裁掉画面外的东西**：播放器外壳、搜索栏、进度条、点赞栏——这些会被一起重绘，必须先裁。
3. **切成小段**：单个视频模型有长度上限（常见 6–10 秒），先确认你选的模型能吃多长。
4. **崩一段只重跑一段**：这是分段的真正价值。就算哪天模型能直出一分钟，也仍然建议切——分段生成可以独立、便宜地重跑。
5. **关键不是切多短，是从哪儿切**：
   - 按**剧情节点**切：「这一句话说完了」「这个动作做完了」的地方。
   - 别按秒数硬切。切在话说到一半的位置，重绘出来人物嘴张着、动作断在半空，跟下一段接不起来，比重切还麻烦。
6. **命名归档**：`part1 / part2...` 放进同一个文件夹，防止弄乱。

---

## 第二步：定立意

不要给角色套一层皮就收工，要让**这场冲突在这个世界里本来就该长成那样**。

自查三问：
- 这个世界有自己的规则吗？（物理、社会、冲突方式）
- 角色在这个世界里为什么会这样行动，而不是原样搬过来？
- 有没有原片没有、但在这个世界里必然发生的细节？

> 参考思路：外语版做得好的，会把整场冲突挪进那种语言的文化语境；动物版会让角色像动物一样叫、一样扑。它们能立住靠的是这个，不是换个滤镜。

---

## 第三步：磨提示词（四层递进）

不要一次性写完整份提示词，一层一层往上加：

**第一层｜统一世界观（全段共用）**
描述整个世界的基底：光色、材质、环境、服装质感。这一层生成出来的东西通常是「能用的底子」，但只是套了层壳。

**第二层｜每段的「本段重点」**
在公共设定之外，给每段追加一句它要突出什么戏：镜头怎么动、什么元素闪烁、人物什么状态。每段有了自己的戏，细节才到位。

**第三层｜台词（单独一份，别混在画面里）**
- **必须扒原片逐句改，不能凭印象编**。照着原片逐句改的世界观版本，每一句都踩在原片那个镜头上，配声音时情绪才对得上。
- **台词要绑死在说话人身上。** 不要只写「某人张嘴大喊」，要写成「某人张嘴大喊『具体台词』」。只写动作不写内容，模型会把台词随机分配给画面里的人——越像应答的短句越容易被分错。

**第四层｜随时迭代**
审片时发现的问题直接补：零碎的小细节（配角服装没跟上、某个元素缺失）用一句自然语言让生成节点的 agent 改；情节走向要动就回头重出整套提示词。

> **实用习惯**：让 AI 把整套提示词**交付成一个 md 文件**（而不是散落在对话里）。这样能在侧边栏直接打开查看，后续继续改的时候把记录顶上去就行，不用反复上滑翻找。

---

## 第四步：重绘与迭代

### 工具交给 AI 推荐，不要替用户拍板
这类工具迭代极快，**skill 里不要写死任何平台名**。由 AI 在了解用户情况后推荐，询问方向：

- 用户手上已经有哪个平台的账号和额度（别让他为了这次重开一个）
- 素材形态：单条精品，还是批量出多版；是否需要反复微调后再生成
- **优先选具备这两个特征的**：背后接了多个视频模型、有「节点画布」
  - 节点画布的价值：把流程搭成一条线之后，换一段视频进去就用同一套设置再跑一遍，**不用每段重新写提示词**
  - 多模型的价值：同一段素材可以横向对比，选到最合适的那个

推荐时给 1–2 个候选并说清各自取舍，让用户自己选。

### 模型怎么选：先问需求，再给建议
**不要上来就推荐某个模型**（模型迭代快，skill 里也不写死具体名字）。先问清用户三项偏好，再据此推荐：

| 要问的 | 典型问法 | 问出来之后 |
| --- | --- | --- |
| 预算敏感度 | 「这次大概愿意花多少？能接受多少份额度试错？」 | 预算紧 → 优先便宜、可多抽卡；预算松 → 可以上贵而稳的 |
| 时间容忍度 | 「急着要，还是能等？」 | 急 → 出片速度排第一；不急 → 可以慢慢挑画质好的 |
| 画质优先级 | 「画质要拉满，还是能看就行？」 | 画质优先 vs 速度优先，直接决定推荐方向 |

**推荐时的通用判断标准**（让用户拿同一句提示词、同一段素材在候选上各跑一次对比）：

1. **出片速度** —— 这类活需要反复改细节，生成一个视频要二十分钟的模型会直接卡死整个流程。当用户明确表示着急或想反复微调时，**这一项往往比画质更决定成败**。
2. **人物稳定性** —— 脸有没有变形、身份有没有保持。
3. **单段成本** —— 决定了能抽几次卡。

推荐完要明确说：**选定后全程不再换**。

### 该「改」还是「重跑」
一直改下去只会越来越偏。以下情况**直接重跑**，不要试图改回去：

- **脸飘了** —— 让它改也改不回原来的长相
- **嘴被画成静止的、但背景还有人在说话** —— 这时候让它张嘴，十有八九口型也对不上
- **角色声音跟以前不一样了** —— 一味改回去只会越来越奇怪

### 音频处理
有台词的片段通常会额外产出一个音频文件，可以直接对它追加指令修改。**实在调不了的音画不同步，把音频保存下来，到最终剪辑里去对齐。**

### 版本管理
每段改完后，把最满意的一版命名为「最终版」，导出时不用在几十个候选里翻。

---

## 第五步：收尾（回到剪辑软件）

别让生成工具干这些精细活：

- 所有片段按序拖进时间轴，拼接、裁掉多余部分
- 段与段之间有色彩或动作跳变 → 加叠化转场
- 音画不同步 → 切割拖动音轨对齐
- 刚才保存的音频在这里用上
- 补音效、BGM、调色、字幕

---

## 提示词模板

画面部分的固定结构：

```
把这段视频改成【风格】风格：【光色与氛围】，
人物服装变成【材质与造型】，背景换成【环境】。
【本段重点：镜头/动作/道具/特效】
【台词：某角色张嘴大喊"具体台词"】
保留人物原本的动作、表情和表演，不要换脸、不要换人。
画面细腻，避免脸部扭曲、肢体变形、文字乱码。
```

最后两句是**任何风格都不能删的固定约束**，它决定人会不会画崩。

| 风格 | 风格词参考 |
| --- | --- |
| 赛博朋克 | 霓虹蓝紫光芒的雨夜、发光纹路、全息屏幕 |
| 废土机甲 | 锈蚀金属、悬浮载具、沙尘与破败工业 |
| 霓虹都市夜景 | 湿漉漉的街道、密集招牌、倒影积水 |
| 暗黑压抑 | 冷蓝主调、潮湿反光地面、体积光 |
| 古典宫廷 | 织锦华服、烛光暖调、木石宫殿 |
| 胶片电影感 | 浅景深、颗粒感、柔和自然光 |

**画面出问题时**，在固定约束那句后面追加：`不要出现多余的手指` / `不要改变人物数量` / `保持背景不动`。

---

## 避坑清单

1. 忘了裁播放器外壳 → 会被一起重绘，成片一眼假
2. 按秒硬切 → 切在半句话上，前后接不起来
3. 中途换模型或换提示词 → 前后段风格不一致
4. 台词不绑说话人 → 模型把词分给别人
5. 脸飘了还在改而不重跑 → 越改越远，白烧额度
6. 反复改剪辑层面的小细节（冗帧、乱码台词、对齐） → 回到剪辑软件几十秒解决，别在生成工具里耗

## 发布前自查

- 人物没明显变形、动作连贯
- 所有分段看起来像同一条片子（风格统一）
- 声音处理到位（原声叠回，或配音完成对口型）
- 包装成套（字幕、BGM、调色、封面一致）
- 标题带上原始话题词 + 你的风格词

---

## 动工前先问用户这些事

分两轮问，别一次性糊十个问题上去。

**第一轮：定方向**
1. 目标：**换世界观**还是**换语言**？想要什么风格？
2. 源视频准备好了吗（本地路径）？有没有必须保留的元素（某句台词、某个符号、原声）？
3. 预算和时限？决定是逐段精修抽卡，还是先跑一版看效果。

**第二轮：定工具与模型**（问完再推荐，别替用户拍板）
4. 手上已经有哪个平台的账号和额度？倾向继续用，还是愿意新开？
5. 预算敏感度：能接受多少试错成本？
6. 时间容忍度：急着要还是能等？（**急 → 出片速度权重最高**）
7. 画质 vs 速度，哪个不能妥协？

根据 4–7 推荐 1–2 个平台与模型候选，说清取舍后让用户确认。

**开工时**
8. 有没有现成的**完整提示词文件**？有就复用；没有就按第三步四层逐步磨出来，并交付成文件。

---

## 配套教程

本 skill 浓缩自一篇完整的实操教程（含成片演示、界面截图、逐段提示词实例），想看作者是怎么一步步做出来的，戳这里：
https://rgg4vktjct.feishu.cn/wiki/DkvHww5akia7uQkyafYcUrxon3b
