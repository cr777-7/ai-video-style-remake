---
name: ai-video-style-remake
description: "把任意已有视频片段做成另一个版本的二创短视频：换画面画风（赛博朋克、水墨、黏土等）、换角色形态（动物版、手办版）、换世界观或时代（天庭版、宫廷版）、换媒介载体（舞台剧版、监控版）、换语种（韩剧版、泰剧版），可任意组合。当用户说「AI重绘这段视频」「风格迁移」「换个世界观」「做个XX版」「做成海外版」「二创」「转绘」「译制」时使用。内含能力边界、开工前询问清单、改动方案设计、六步流程、分层提示词法、平台与模型选型、改与重跑的判断、收尾清单、合规提醒。"
agent_created: true
---

# 视频二创：做一个新版本

把一段已有的视频，做成另一个版本的成片。适用于影视剧片段、短剧、口播、UGC 素材。

**前提：这类二创的画面几乎都要动。** 只换声音不动画面，出来就是个配音版，一眼假。区别只在于画面动多大幅度。

**另一个前提：流程是来回循环，不是一路走到底。** 磨提示词和跑重绘之间会反复好几轮（跑一版 → 审片 → 改 → 再跑）。下面列的顺序是第一次走的顺序，不是只走一遍。

---

## 0. 先划清：你能替用户做什么，不能替他做什么

**这条最重要，先读。**

**你（AI）能做的**
- 帮用户设计方案：改哪些维度、画面动多大幅度
- 定切段点、判断该重跑还是继续改
- 按四层结构把提示词磨出来，**并交付成一个 md 文件**
- 根据用户的预算、时间、画质偏好，推荐平台和模型的候选
- 给出收尾清单、发布自查清单
- 用户卡住时，告诉他下一步该做什么

**你做不到的，别硬装**
- **你看不到视频内容**。切点、台词、画面里有什么，全都要用户告诉你。不要凭原片的名字去脑补台词——**「扒原片逐句改」这件事只能由用户提供台词或字幕，你负责改写**
- 替用户上传素材、点生成、付费 —— 必须由用户在平台上操作
- 「当下有哪些平台、各自多少钱」这类实时信息不要凭印象编。需要推荐时先联网查并注明「以实际为准」；查不到就直接让用户说他手上有哪些账号
- 各平台里同一个功能的名字一直在变，别假装精确知道某个按钮在哪。**说清功能本质**（「支持给已有视频提修改要求的入口」），让用户自己对照着找

**前置条件**：用户需要有某个视频生成平台的账号，并且会用最基本的剪辑操作（切段、拖时间轴、导音频）。两样都没有就先补，别直接开工。

---

## 1. 开工前必须问清的事

分两轮问，**别一次性糊十个问题上去**。

**第一轮：定方向**
1. 想做一个什么版本？（换画风 / 换角色形态 / 换世界观时代 / 换媒介载体 / 换语种，还是几样一起）说得出具体版本名最好
2. 源视频准备好了吗？本地路径是什么？
3. **这段视频里有什么？** 有几个人物、大概什么动作、有没有台词。有台词的话请用户提供原文或字幕（**你看不到画面，必须由用户提供**）
4. 预算和时限？决定是逐段精修抽卡，还是先跑一版看效果

**第二轮：定工具与模型**（问完再推荐，**别替用户拍板**）
5. 手上已经有哪个平台的账号和额度？倾向继续用，还是愿意新开？
6. 预算敏感度：能接受多少试错成本？
7. 时间容忍度：急着要还是能等？（**急 → 出片速度权重最高**）
8. 画质 vs 速度，哪个不能妥协？

根据 5–8 推荐 1–2 个候选并说清取舍，让用户确认后再往下走。

> **顺带提醒一次合规**（详见第 9 节）：影视剧片段、综艺、名人素材通常受版权保护。个人练手、小范围分享一般没事，商用和带货风险明显更高。让用户自己判断尺度，你不要教人怎么规避检测。

**问完之后立刻判断画面幅度**（见第 4 节），再进入流程。

---

## 2. 三条铁律

**1. 喂原片重绘，不用纯文字生成。**
纯靠提示词生成，人是模型现画的——脸不是本人，一动起来更不像；动作和表演也是它自己演的，什么时候逼近、哪一句拔高音量它全不知道，节奏必然对不上。喂原片重绘是在本人画面上换画风，**脸是本脸，动作、走位、节奏天然跟原片一致，每段长相也天然统一**，提示词只需要写「改成什么样」，不用描述怎么演。

**2. AI 负责把画面生出来，精细活用户自己做。**
冗帧、乱码台词、音画不同步这类问题，跟模型反复扯皮既费时间又费积分，回到剪辑软件通常几十秒解决。**改两轮还不对就停手**，交给剪辑，别死磕。

**3. 锁底层，放局部。**
锁的是**世界观底层描述、模型、负向约束**——这三层必须全段一致，中途换会让前后段串味。
放的是**每段的「本段重点」**——每段本来就该有自己的戏，这不是串味，是必需的。

---

## 3. 术语速览

| 说法 | 意思 |
| --- | --- |
| 重绘 / 风格迁移 / 转绘 | 保留原片的人和结构，用 AI 把画面按新画风重新画一遍 |
| 译制 / 换声 | 把说话的语言换成外语并做对口型；**画面仍要配合改**（调色、台标、字幕、BGM） |
| 抽卡 | 同一段用同一句提示词跑几次，挑最好的那一版（每次都花额度和时间） |
| 节点画布 | 部分平台提供的工作流界面：把「上传 → 提示词 → 生成」搭成一条线，换素材可复用同一套设置 |
| agent | 平台里能用自然语言对某段视频或某条音频追加修改指令的入口 |
| 样片 | 只拿一段跑通全流程，用来验证风格和稳定性，避免全铺开后发现不对 |

---

## 4. 先定改动方案：改哪些维度 + 画面动多大

不要纠结「重绘还是译制」这种二分法。**一个版本往往同时改好几层**，直接按下面两张表定方案。

### 改哪些维度（可多选）

| 维度 | 改的是什么 | 例子 |
| --- | --- | --- |
| 语种 | 说话的语言 | 韩剧版、泰剧版、英语版 |
| 画面画风 | 整体视觉质感 | 赛博朋克、水墨、油画、黏土 |
| 角色形态 | 角色本身变成什么 | 动物版、公仔手办版、像素小人版 |
| 世界观 / 时代 | 故事发生的世界 | 天庭版、仙侠版、宫廷版、末日版 |
| 媒介载体 | 用什么形式呈现 | 舞台剧版、游戏CG版、监控视角版 |
| 时代画质 | 像哪个年代拍的 | 老电影、DV录像、默片、CRT |

**一次主打一到两层。** 层数越多，成本和失败率越高。

### 画面动多大（按预算和时限选）

| 幅度 | 做什么 | 适合 |
| --- | --- | --- |
| 轻度 | 只做调色、加台标和字幕样式、换 BGM，画面结构不动 | 预算低、时间紧、原片够干净 |
| 中度 | 局部重绘：换背景、服装、道具，人物保留 | 想有明显变化但预算有限 |
| 重度 | 整体重绘：换世界观或角色形态 | 要视觉冲击、要规避查重 |

> **语种类版本通常落在轻度到中度** —— 画面必须动，只是动多少的问题。只配音不动画面，一眼就是配音版。

### 多维度组合时的固定顺序

**先做画面 → 再做语种 → 最后统一包装。**
顺序不能反：先译制再重绘，口型会被重绘覆盖掉。

**幅度为轻度时**，可以直接跳过流程里的重绘环节：切段和备料可以简化，重点放在步骤 6 的包装上。

---

## 5. 六步流程

### 步骤 1｜备料与切段

1. **拿到源视频**：优先从原链接保存；只能录屏时就录屏（Mac 录屏默认落在桌面，命名类似 `录屏YYYY-MM-DD HH.MM.SS.mov`）。
2. **裁掉画面外的东西**：播放器外壳、搜索栏、进度条、点赞栏——这些会被一起重绘，必须先裁。
3. **切成小段**：以所选模型的长度上限为准（通常是**个位数秒**，不同模型不同，先确认再切）。
4. **崩一段只重跑一段**：这是分段的真正价值。就算哪天模型能直出一分钟也建议切——分段可以独立、便宜地重跑。
5. **关键不是切多短，是从哪儿切**：
   - 按**剧情节点**切：「这一句话说完了」「这个动作做完了」的地方。
   - 别按秒数硬切。切在话说到一半的位置，重绘出来人物嘴张着、动作断在半空，跟下一段接不起来。
6. **命名归档**：`part1 / part2...` 放进同一个文件夹。

### 步骤 2｜定立意

不要给角色套一层皮就收工，要让**这场冲突在这个新版本里本来就该长成那样**。

自查三问：
- 这个世界 / 这个载体有自己的规则吗？
- 角色在这个版本里为什么会这样行动，而不是原样搬过来？
- 有没有原片没有、但在这个版本里必然发生的细节？

> 参考：外语版做得好的，会把整场冲突挪进那种语言的文化语境，甚至加入原片没有的情节；动物版会让角色像动物一样叫、一样扑。它们能立住靠的是这个，不是换个滤镜。

### 步骤 3｜建项目、传素材、选模型

1. **建一个专门的项目或画布**，为这次创作单独准备（别混在旧项目里）。
2. **把所有分段都传上去**（不是只传一段）。先传完，再挑一段跑样片。
3. **选模型**：按第 1 节问到的偏好来。判断标准：
   - **出片速度** —— 这类活要反复改细节，一个视频要二十分钟的模型会直接卡死流程。用户急着要或要反复微调时，**这一项往往比画质更决定成败**
   - **人物稳定性** —— 脸有没有变形、身份有没有保持
   - **单段成本** —— 决定能抽几次卡
4. **拿样片对比**（这一步不能省）：用最精彩的那一段，同一句提示词在不同模型上各跑一次，选定了就**全程不再换**。

**优先选具备这两个特征的平台**
- 背后接了多个视频模型：可以横向对比
- 有「节点画布」：流程搭成一条线之后，换一段视频进去就用同一套设置再跑一遍，**不用每段重新写提示词**

### 步骤 4｜磨提示词（四层递进）

不要一次性写完整份提示词，一层一层往上加：

**第一层｜统一世界观（全段共用，锁死）**
描述整个世界的基底：光色、材质、环境、服装质感。跑出来通常是「能用的底子」，但只是套了层壳。

**第二层｜每段的「本段重点」（每段不同，这是正常的）**
在公共设定之外，给每段追加一句它要突出什么戏：镜头怎么动、什么元素闪烁、人物什么状态。

**第三层｜台词**
- **必须照原片逐句改，不能凭印象编**。改出来的每一句都踩在原片那个镜头上，配声音时情绪才对得上。
- 台词原文**只能由用户提供**（你看不到画面）。拿到后按新版本的说法改写。
- **台词要绑死在说话人身上**：不要只写「某人张嘴大喊」，要写成「某人张嘴大喊『具体台词』」。只写动作不写内容，模型会把台词随机分给画面里的人——越像应答的短句越容易被分错。

**第四层｜随时迭代**
审片发现的问题直接补：零碎小细节（配角服装没跟上、某个元素缺失）用一句自然语言让 agent 改；情节走向要动就回头重出整套提示词。

> **必须做的事**：把整套提示词**交付成一个 md 文件**，而不是散落在对话里。用户要边看边改，散在聊天记录里根本没法用。

### 步骤 5｜重绘与迭代

**该「改」还是「重跑」**
一直改下去只会越来越偏。以下情况**直接重跑**：
- **脸飘了** —— 让它改也改不回原来的长相
- **嘴被画成静止的、但背景还有人在说话** —— 让它张嘴，十有八九口型也对不上
- **角色声音跟以前不一样了** —— 一味改回去只会越来越奇怪

**音频**
有台词的片段可能额外产出一个音频文件，可以对它追加指令修改。**实在调不了的音画不同步，把音频保存下来，到步骤 6 里去对齐。**

**版本管理**
每段改完后，把最满意的一版命名为「最终版」，导出时不用在几十个候选里翻。

**某段死活跑不好，按顺序退**
1. 换一种描述方式重写这段提示词（可能原句有歧义）
2. 把这段再切短（可能信息量超载）
3. 直接剪掉这段或用原片顶上 —— 一段不影响全局，死磕它的成本远大于收益

### 步骤 6｜收尾（回到剪辑软件）

别让生成工具干这些精细活：

- 所有片段按序拖进时间轴，拼接、裁掉多余部分
- 段与段之间有色彩或动作跳变 → 加叠化转场
- 音画不同步 → 切割拖动音轨对齐
- 步骤 5 保存的音频在这里用上
- 补音效、BGM、调色、字幕；不同版本的包装各有讲究：舞台剧加幕布和掌声，监控加时间码和录像 UI，游戏 CG 加血条技能栏，语种版加台标和该语种的字幕样式

---

## 6. 提示词怎么写：五要素齐全，别套模板

**不要拿示例当填空题做。** 句式、顺序、长短都不重要，重要的是这五样齐全：

1. **对象**：改的是这段视频里的什么（整体画面 / 某个角色 / 背景 / 某个道具）
2. **目标版本**：改成什么样（越具体越好，别只给一个风格词）
3. **必留**：必须原样保留什么 —— 保脸 / 保动作表情 / 保背景结构 / 保表演节奏 / 保 logo，按需勾选。**这一条最该跟着素材变**：空镜、产品、静物这类素材，人脸相关的保留项直接删掉
4. **可辨识特征**：两三条让人一眼认出是这个版本的具体要素（不是形容词，是看得见的东西）
5. **负向约束**：不要出现什么

### 不同维度该在提示词里补什么

| 改的维度 | 第 4 条「可辨识特征」重点写什么 |
| --- | --- |
| 画面画风 | 光色、材质、环境纹理 |
| 角色形态 | **逐角色**写形态、服装、道具，别笼统写「人物变成…」 |
| 世界观 / 时代 | 环境、建筑、服装道具、标志性元件 |
| 媒介载体 | 该载体的画面特征（机位、畸变、颗粒、UI 叠加） |
| 时代画质 | 分辨率、颗粒、抖动、色彩偏差 |
| 语种 | 画面部分同上；**另外单独做译制 + 文化语境改写 + 包装**（不是提示词能解决的） |

### 一句示范（示意结构，不是模板）

```
把这段视频改成【目标版本】：【可辨识特征2-3条】，
【逐角色说明：谁变成什么】。镜头【景别/运镜】，
【本段重点：动效或情节】
【台词：某角色张嘴大喊"具体台词"】
保留【必留项：动作、表情、表演节奏】。
避免【负向约束：脸部扭曲、肢体变形、文字乱码】。
```

**画面出问题时**，补在负向约束后面：
`不要出现多余的手指` / `不要改变人物数量` / `保持背景不动` / `不要新增文字招牌` / `保持镜头运动方向一致`

**语种的坑**：台词分错人 → 把台词写进动作里绑死说话人；口型对不上 → 缩短句子或调整断句后重跑

### 参考词（起手用，别被它框住）

**画风**：赛博朋克（霓虹蓝紫雨夜、发光纹路、全息屏）／废土机甲（锈蚀金属、悬浮载具）／水墨国风（留白、晕染、淡彩）／黏土定格（哑光、指痕、微抖动）

**载体**：舞台剧（顶光、幕布、观众席暗部）／监控（广角畸变、低清、时间戳）／老胶片（颗粒、刮痕、抖动）／游戏CG（高对比光照、UI）

**形态**：毛绒动物、公仔手办、像素小人、木偶、纸片人

**世界/时代**：仙侠天庭、宫廷、民国、末日废墟、校园

想要别的直接用自然语言描述 —— 这张表只是起个头，不是菜单。

---

## 7. 避坑清单

1. 忘了裁播放器外壳 → 会被一起重绘，成片一眼假
2. 按秒硬切 → 切在半句话上，前后接不起来
3. 不做样片就全铺开 → 风格不对时全部白跑
4. 中途换模型或换底层世界观 → 前后段串味（注意：每段「本段重点」不同是正常的，不在此列）
5. 台词不绑说话人 → 模型把词分给别人
6. 脸飘了还在改而不重跑 → 越改越远，白烧额度
7. 只配音不动画面 → 一眼就是配音版
8. 拿捏不准平台行情就硬推荐 → 先联网查，或让用户自己说他有哪些账号
9. 跟模型较劲改剪辑层面的细节 → 回剪辑软件几十秒解决

---

## 8. 发布前自查

- 人物没明显变形、动作连贯
- 所有分段看起来像同一条片子（底层设定一致）
- 声音处理到位（原声叠回，或配音完成对口型）
- 包装成套（字幕、BGM、调色、台标、封面一致）
- 标题带上原始话题词 + 自己的版本词

---

## 9. 合规提醒

影视剧片段、综艺、名人素材通常受版权保护。个人练手、小范围分享一般没事，但：

- **商用、带货、接推广**这类盈利用途风险明显更高
- 平台可能因侵权投诉下架或限流
- 涉及真人形象时，改得越像本人越容易被判定为不当使用

提醒到位即可，让用户自己判断尺度。**不要教人怎么规避侵权检测。**

---

## 10. 配套教程

本 skill 浓缩自一篇完整的实操教程（含成片演示、界面截图、逐段提示词实例）：
https://rgg4vktjct.feishu.cn/wiki/DkvHww5akia7uQkyafYcUrxon3b
